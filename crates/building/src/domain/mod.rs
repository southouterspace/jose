//! The building domain: entities and value objects for semantic promotion and per-instance
//! placement. Pure — depends only on the geometry-kernel and the materials context (by key).

pub mod assemblies;
pub mod placement;
pub mod spacing;
pub mod wall;
